# Install brewfile with 'brew bundle Brewfile'

# Make sure we’re using the latest Homebrew
update

# Upgrade any already-installed formulae
upgrade

#tap homebrew/versions
#install lua52

# install network & build tools
install ssh-copy-id
install bazaar mercurial
install ec2-ami-tools ec2-api-tools
install node

# install general utilities
install a2ps

# install flac tools
install flac shntool cuetools atomicparsley

# Remove outdated versions from the cellar
cleanup
